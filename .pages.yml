content:
  - name: posts
    label: Posts
    path: _posts
    type: collection
    filename: '{year}-{month}-{day}-{primary}.md'
    view:
      fields: [ title, date ]
    fields:
      - name: published
        label: Published
        type: boolean
        default: true
      - name: date
        label: Date
        type: date
      - name: title
        label: Title
        type: string
      - name: body
        label: Body
        type: rich-text
      - name: tags
        label: Tags
        type: string
        list: true
  - name: config
    label: Jekyll config
    path: _config.yml
    type: file
    fields:
      - name: title
        label: Website title
        type: string
      - name: description
        label: Website description
        type: string
        description: Will be provused for any page with no description.
      - name: url
        label: Website URL
        type: string
        pattern: ^(https?:\/\/)?(www\.)?[a-zA-Z0-9.-]+\.[a-zA-Z]{2,}(\/[^\s]*)?$
      - name: baseurl
        label: Base Link
        type: string
        default: ""
        description: the blog's "icon" link
      - name: cover
        label: Preview image
        type: image
        description: Image used in the social preview on social networks (e.g. Facebook, Twitter...)
      - name: theme
        label: Theme
        type: string
      - name: gems
        label: Gems (build settings)
        type: string
        list: true
      - name: exclude
        label: Exclude (build settings)
        type: string
        list: true
  - name: cname
    label: CNAME domain
    path: CNAME
    type: file
    fields:
      - name: domain
        label: Domain
        type: string